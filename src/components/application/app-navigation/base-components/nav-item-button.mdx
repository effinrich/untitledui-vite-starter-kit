import { Canvas, Meta, ArgTypes } from '@storybook/blocks'
import * as NavItemButtonStories from './nav-item-button.stories'

<Meta of={NavItemButtonStories} />

# NavItemButton

The `NavItemButton` component is a reusable UI element.

## Usage

### Basic Example

<Canvas of={NavItemButtonStories.Default} />

```tsx
<NavItemButton>
  Content
</NavItemButton>
```

## Props

<ArgTypes of={NavItemButtonStories} />

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `open` | `boolean` | `-` | - |
| `href` | `string` | `-` | - |
| `label` | `string` | `-` | - |
| `icon` | `FC<{ className?: string` | `-` | - |


## Accessibility

The `NavItemButton` component follows WAI-ARIA guidelines:

- Supports keyboard navigation
- Includes proper ARIA attributes
- Compatible with screen readers

### Keyboard Interactions

| Key | Description |
|-----|-------------|
| `Tab` | Moves focus to the component |
| `Enter` | Activates the component |
| `Space` | Activates the component |

## Composition

### With Other Components

```tsx
<div className="flex flex-col gap-4">
  <NavItemButton>
    First item
  </NavItemButton>
  <NavItemButton>
    Second item
  </NavItemButton>
</div>
```

## Responsive Design

Use CSS media queries for responsive behavior:

```tsx
<NavItemButton
  
  style={{ width: '100%' }}
  className="responsive-component"
>
  Responsive NavItemButton
</NavItemButton>

<style>
  @media (min-width: 768px) {
    .responsive-component {
      width: auto;
      
    }
  }
</style>
```

## Best Practices

1. **Accessibility**: Always provide meaningful content or aria-label
4. **Composition**: Combine with other components using Stack or Flex for layouts
5. **Performance**: Avoid creating new callback functions on each render

## Related Components

- See your design system documentation for related components
