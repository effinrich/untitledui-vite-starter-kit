import { Canvas, Meta, ArgTypes } from '@storybook/blocks'
import * as NavListStories from './nav-list.stories'

<Meta of={NavListStories} />

# NavList

The `NavList` component is a reusable UI element.

## Usage

### Basic Example

<Canvas of={NavListStories.Default} />

```tsx
<NavList>
  Content
</NavList>
```

## Props

<ArgTypes of={NavListStories} />

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `activeUrl` | `string` | `-` | - |
| `className` | `string` | `-` | - |
| `items` | `(NavItemType \| NavItemDividerType)[]` | `-` | - |


## Accessibility

The `NavList` component follows WAI-ARIA guidelines:

- Supports keyboard navigation
- Includes proper ARIA attributes
- Compatible with screen readers

### Keyboard Interactions

| Key | Description |
|-----|-------------|
| `Tab` | Moves focus to the component |
| `Enter` | Activates the component |
| `Space` | Activates the component |

## Composition

### With Other Components

```tsx
<div className="flex flex-col gap-4">
  <NavList>
    First item
  </NavList>
  <NavList>
    Second item
  </NavList>
</div>
```

## Responsive Design

Use CSS media queries for responsive behavior:

```tsx
<NavList
  
  style={{ width: '100%' }}
  className="responsive-component"
>
  Responsive NavList
</NavList>

<style>
  @media (min-width: 768px) {
    .responsive-component {
      width: auto;
      
    }
  }
</style>
```

## Best Practices

1. **Accessibility**: Always provide meaningful content or aria-label
4. **Composition**: Combine with other components using Stack or Flex for layouts
5. **Performance**: Avoid creating new callback functions on each render

## Related Components

- See your design system documentation for related components
