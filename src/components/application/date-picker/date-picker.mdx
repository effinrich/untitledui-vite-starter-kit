import { Canvas, Meta, ArgTypes } from '@storybook/blocks'
import * as DatePickerStories from './date-picker.stories'

<Meta of={DatePickerStories} />

# DatePicker

The `DatePicker` component is a reusable UI element.

## Usage

### Basic Example

<Canvas of={DatePickerStories.Default} />

```tsx
<DatePicker>
  Content
</DatePicker>
```

## Props

<ArgTypes of={DatePickerStories} />

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `onApply` | `() => void` | `-` | - |
| `onCancel` | `() => void` | `-` | - |


## Accessibility

The `DatePicker` component follows WAI-ARIA guidelines:

- Supports keyboard navigation
- Includes proper ARIA attributes
- Compatible with screen readers

### Keyboard Interactions

| Key | Description |
|-----|-------------|
| `Tab` | Moves focus to the component |
| `Enter` | Activates the component |
| `Space` | Activates the component |

## Composition

### With Other Components

```tsx
<div className="flex flex-col gap-4">
  <DatePicker>
    First item
  </DatePicker>
  <DatePicker>
    Second item
  </DatePicker>
</div>
```

## Responsive Design

Use CSS media queries for responsive behavior:

```tsx
<DatePicker
  
  style={{ width: '100%' }}
  className="responsive-component"
>
  Responsive DatePicker
</DatePicker>

<style>
  @media (min-width: 768px) {
    .responsive-component {
      width: auto;
      
    }
  }
</style>
```

## Best Practices

1. **Accessibility**: Always provide meaningful content or aria-label
4. **Composition**: Combine with other components using Stack or Flex for layouts
5. **Performance**: Avoid creating new callback functions on each render

## Related Components

- See your design system documentation for related components
