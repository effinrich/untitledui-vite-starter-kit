import { Canvas, Meta, ArgTypes } from '@storybook/blocks'
import * as DateRangePickerStories from './date-range-picker.stories'

<Meta of={DateRangePickerStories} />

# DateRangePicker

The `DateRangePicker` component is a reusable UI element.

## Usage

### Basic Example

<Canvas of={DateRangePickerStories.Default} />

```tsx
<DateRangePicker>
  Content
</DateRangePicker>
```

## Props

<ArgTypes of={DateRangePickerStories} />

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `onApply` | `() => void` | `-` | - |
| `onCancel` | `() => void` | `-` | - |


## Accessibility

The `DateRangePicker` component follows WAI-ARIA guidelines:

- Supports keyboard navigation
- Includes proper ARIA attributes
- Compatible with screen readers

### Keyboard Interactions

| Key | Description |
|-----|-------------|
| `Tab` | Moves focus to the component |
| `Enter` | Activates the component |
| `Space` | Activates the component |

## Composition

### With Other Components

```tsx
<div className="flex flex-col gap-4">
  <DateRangePicker>
    First item
  </DateRangePicker>
  <DateRangePicker>
    Second item
  </DateRangePicker>
</div>
```

## Responsive Design

Use CSS media queries for responsive behavior:

```tsx
<DateRangePicker
  
  style={{ width: '100%' }}
  className="responsive-component"
>
  Responsive DateRangePicker
</DateRangePicker>

<style>
  @media (min-width: 768px) {
    .responsive-component {
      width: auto;
      
    }
  }
</style>
```

## Best Practices

1. **Accessibility**: Always provide meaningful content or aria-label
4. **Composition**: Combine with other components using Stack or Flex for layouts
5. **Performance**: Avoid creating new callback functions on each render

## Related Components

- See your design system documentation for related components
