import { Canvas, Meta, ArgTypes } from '@storybook/blocks'
import * as EmptyStateStories from './empty-state.stories'

<Meta of={EmptyStateStories} />

# EmptyState

The `EmptyState` component is a reusable UI element.

## Usage

### Basic Example

<Canvas of={EmptyStateStories.Default} />

```tsx
<EmptyState>
  Content
</EmptyState>
```

## Props

<ArgTypes of={EmptyStateStories} />

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `Title` | `typeof Title` | `-` | - |
| `Header` | `typeof Header` | `-` | - |
| `Footer` | `typeof Footer` | `-` | - |
| `Content` | `typeof Content` | `-` | - |
| `Description` | `typeof Description` | `-` | - |
| `Illustration` | `typeof Illustration` | `-` | - |
| `FeaturedIcon` | `typeof FeaturedIcon` | `-` | - |
| `FileTypeIcon` | `typeof FileTypeIcon` | `-` | - |


## Accessibility

The `EmptyState` component follows WAI-ARIA guidelines:

- Supports keyboard navigation
- Includes proper ARIA attributes
- Compatible with screen readers

### Keyboard Interactions

| Key | Description |
|-----|-------------|
| `Tab` | Moves focus to the component |
| `Enter` | Activates the component |
| `Space` | Activates the component |

## Composition

### With Other Components

```tsx
<div className="flex flex-col gap-4">
  <EmptyState>
    First item
  </EmptyState>
  <EmptyState>
    Second item
  </EmptyState>
</div>
```

## Responsive Design

Use CSS media queries for responsive behavior:

```tsx
<EmptyState
  
  style={{ width: '100%' }}
  className="responsive-component"
>
  Responsive EmptyState
</EmptyState>

<style>
  @media (min-width: 768px) {
    .responsive-component {
      width: auto;
      
    }
  }
</style>
```

## Best Practices

1. **Accessibility**: Always provide meaningful content or aria-label
4. **Composition**: Combine with other components using Stack or Flex for layouts
5. **Performance**: Avoid creating new callback functions on each render

## Related Components

- See your design system documentation for related components
