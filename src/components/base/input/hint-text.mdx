import { Canvas, Meta, ArgTypes } from '@storybook/blocks'
import * as HintTextStories from './hint-text.stories'

<Meta of={HintTextStories} />

# HintText

The `HintText` component is a reusable UI element.

## Usage

### Basic Example

<Canvas of={HintTextStories.Default} />

```tsx
<HintText>
  Content
</HintText>
```

## Props

<ArgTypes of={HintTextStories} />

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `isInvalid` | `boolean` | `-` | - |
| `ref` | `Ref<HTMLElement>` | `-` | - |
| `children` | `ReactNode` | `-` | - |


## Accessibility

The `HintText` component follows WAI-ARIA guidelines:

- Supports keyboard navigation
- Includes proper ARIA attributes
- Compatible with screen readers

### Keyboard Interactions

| Key | Description |
|-----|-------------|
| `Tab` | Moves focus to the component |
| `Enter` | Activates the component |
| `Space` | Activates the component |

## Composition

### With Other Components

```tsx
<div className="flex flex-col gap-4">
  <HintText>
    First item
  </HintText>
  <HintText>
    Second item
  </HintText>
</div>
```

## Responsive Design

Use CSS media queries for responsive behavior:

```tsx
<HintText
  
  style={{ width: '100%' }}
  className="responsive-component"
>
  Responsive HintText
</HintText>

<style>
  @media (min-width: 768px) {
    .responsive-component {
      width: auto;
      
    }
  }
</style>
```

## Best Practices

1. **Accessibility**: Always provide meaningful content or aria-label
4. **Composition**: Combine with other components using Stack or Flex for layouts
5. **Performance**: Avoid creating new callback functions on each render

## Related Components

- See your design system documentation for related components
