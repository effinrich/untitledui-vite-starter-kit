import { Canvas, Meta, ArgTypes } from '@storybook/blocks'
import * as FeaturedIconStories from './featured-icon.stories'

<Meta of={FeaturedIconStories} />

# FeaturedIcon

The `FeaturedIcon` component is a reusable UI element.

## Usage

### Basic Example

<Canvas of={FeaturedIconStories.Default} />

```tsx
<FeaturedIcon>
  Content
</FeaturedIcon>
```

## Props

<ArgTypes of={FeaturedIconStories} />

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `ref` | `Ref<HTMLDivElement>` | `-` | - |
| `children` | `ReactNode` | `-` | - |
| `className` | `string` | `-` | - |
| `icon` | `FC<{ className?: string` | `-` | - |


## Accessibility

The `FeaturedIcon` component follows WAI-ARIA guidelines:

- Supports keyboard navigation
- Includes proper ARIA attributes
- Compatible with screen readers

### Keyboard Interactions

| Key | Description |
|-----|-------------|
| `Tab` | Moves focus to the component |
| `Enter` | Activates the component |
| `Space` | Activates the component |

## Composition

### With Other Components

```tsx
<div className="flex flex-col gap-4">
  <FeaturedIcon>
    First item
  </FeaturedIcon>
  <FeaturedIcon>
    Second item
  </FeaturedIcon>
</div>
```

## Responsive Design

Use CSS media queries for responsive behavior:

```tsx
<FeaturedIcon
  
  style={{ width: '100%' }}
  className="responsive-component"
>
  Responsive FeaturedIcon
</FeaturedIcon>

<style>
  @media (min-width: 768px) {
    .responsive-component {
      width: auto;
      
    }
  }
</style>
```

## Best Practices

1. **Accessibility**: Always provide meaningful content or aria-label
4. **Composition**: Combine with other components using Stack or Flex for layouts
5. **Performance**: Avoid creating new callback functions on each render

## Related Components

- See your design system documentation for related components
