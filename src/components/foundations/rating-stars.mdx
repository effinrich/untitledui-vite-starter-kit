import { Canvas, Meta, ArgTypes } from '@storybook/blocks'
import * as RatingStarsStories from './rating-stars.stories'

<Meta of={RatingStarsStories} />

# RatingStars

The `RatingStars` component is a reusable UI element.

## Usage

### Basic Example

<Canvas of={RatingStarsStories.Default} />

```tsx
<RatingStars>
  Content
</RatingStars>
```

## Props

<ArgTypes of={RatingStarsStories} />

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `rating` | `number` | `-` | - |
| `stars` | `number` | `-` | - |
| `starClassName` | `string` | `-` | - |


## Accessibility

The `RatingStars` component follows WAI-ARIA guidelines:

- Supports keyboard navigation
- Includes proper ARIA attributes
- Compatible with screen readers

### Keyboard Interactions

| Key | Description |
|-----|-------------|
| `Tab` | Moves focus to the component |
| `Enter` | Activates the component |
| `Space` | Activates the component |

## Composition

### With Other Components

```tsx
<div className="flex flex-col gap-4">
  <RatingStars>
    First item
  </RatingStars>
  <RatingStars>
    Second item
  </RatingStars>
</div>
```

## Responsive Design

Use CSS media queries for responsive behavior:

```tsx
<RatingStars
  
  style={{ width: '100%' }}
  className="responsive-component"
>
  Responsive RatingStars
</RatingStars>

<style>
  @media (min-width: 768px) {
    .responsive-component {
      width: auto;
      
    }
  }
</style>
```

## Best Practices

1. **Accessibility**: Always provide meaningful content or aria-label
4. **Composition**: Combine with other components using Stack or Flex for layouts
5. **Performance**: Avoid creating new callback functions on each render

## Related Components

- See your design system documentation for related components
