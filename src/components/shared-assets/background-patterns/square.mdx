import { Canvas, Meta, ArgTypes } from '@storybook/blocks'
import * as SquareStories from './square.stories'

<Meta of={SquareStories} />

# Square

The `Square` component provides configurable sizes.

## Usage

### Basic Example

<Canvas of={SquareStories.Default} />

```tsx
<Square size="md">
  Content
</Square>
```

### Sizes

Available in 3 sizes:

<Canvas of={SquareStories.Sizes} />

```tsx
<Square size="sm">sm</Square>
<Square size="md">md</Square>
<Square size="lg">lg</Square>
```

## Props

<ArgTypes of={SquareStories} />

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `size` | `"sm" \| "md" \| "lg"` | `-` | - |


## Accessibility

The `Square` component follows WAI-ARIA guidelines:

- Supports keyboard navigation
- Includes proper ARIA attributes
- Compatible with screen readers

### Keyboard Interactions

| Key | Description |
|-----|-------------|
| `Tab` | Moves focus to the component |
| `Enter` | Activates the component |
| `Space` | Activates the component |

## Composition

### With Other Components

```tsx
<div className="flex flex-col gap-4">
  <Square>
    First item
  </Square>
  <Square>
    Second item
  </Square>
</div>
```

## Responsive Design

Use CSS media queries for responsive behavior:

```tsx
<Square
  size="sm"
  style={{ width: '100%' }}
  className="responsive-component"
>
  Responsive Square
</Square>

<style>
  @media (min-width: 768px) {
    .responsive-component {
      width: auto;
      /* Use md size */
    }
  }
</style>
```

## Best Practices

1. **Accessibility**: Always provide meaningful content or aria-label
3. **Sizing**: Use consistent sizes within the same section of your UI
4. **Composition**: Combine with other components using Stack or Flex for layouts
5. **Performance**: Avoid creating new callback functions on each render

## Related Components

- See your design system documentation for related components
