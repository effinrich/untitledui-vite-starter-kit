import { Canvas, Meta, ArgTypes } from '@storybook/blocks'
import * as NotFoundStories from './not-found.stories'

<Meta of={NotFoundStories} />

# NotFound

The `NotFound` component is a reusable UI element.

## Usage

### Basic Example

<Canvas of={NotFoundStories.Default} />

```tsx
<NotFound>
  Content
</NotFound>
```

## Props

<ArgTypes of={NotFoundStories} />

This component accepts standard HTML attributes.

## Accessibility

The `NotFound` component follows WAI-ARIA guidelines:

- Supports keyboard navigation
- Includes proper ARIA attributes
- Compatible with screen readers

### Keyboard Interactions

| Key | Description |
|-----|-------------|
| `Tab` | Moves focus to the component |
| `Enter` | Activates the component |
| `Space` | Activates the component |

## Integration Notes

### Router Integration

This component uses React Router. Ensure it's wrapped in a Router provider:

```tsx
import { BrowserRouter } from 'react-router-dom'

<BrowserRouter>
  <YourComponent />
</BrowserRouter>
```

## Composition

### With Other Components

```tsx
<div className="flex flex-col gap-4">
  <NotFound>
    First item
  </NotFound>
  <NotFound>
    Second item
  </NotFound>
</div>
```

## Responsive Design

Use CSS media queries for responsive behavior:

```tsx
<NotFound
  
  style={{ width: '100%' }}
  className="responsive-component"
>
  Responsive NotFound
</NotFound>

<style>
  @media (min-width: 768px) {
    .responsive-component {
      width: auto;
      
    }
  }
</style>
```

## Best Practices

1. **Accessibility**: Always provide meaningful content or aria-label
4. **Composition**: Combine with other components using Stack or Flex for layouts
5. **Performance**: Avoid creating new callback functions on each render

## Related Components

- See your design system documentation for related components
