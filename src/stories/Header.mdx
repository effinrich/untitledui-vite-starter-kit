import { Canvas, Meta, ArgTypes } from '@storybook/blocks'
import * as HeaderStories from './Header.stories'

<Meta of={HeaderStories} />

# Header

The `Header` component is a reusable UI element.

## Usage

### Basic Example

<Canvas of={HeaderStories.Default} />

```tsx
<Header>
  Content
</Header>
```

## Props

<ArgTypes of={HeaderStories} />

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `user` | `User` | `-` | - |
| `onLogin` | `() => void` | `-` | - |
| `onLogout` | `() => void` | `-` | - |
| `onCreateAccount` | `() => void` | `-` | - |


## Accessibility

The `Header` component follows WAI-ARIA guidelines:

- Supports keyboard navigation
- Includes proper ARIA attributes
- Compatible with screen readers

### Keyboard Interactions

| Key | Description |
|-----|-------------|
| `Tab` | Moves focus to the component |
| `Enter` | Activates the component |
| `Space` | Activates the component |

## Composition

### With Other Components

```tsx
<div className="flex flex-col gap-4">
  <Header>
    First item
  </Header>
  <Header>
    Second item
  </Header>
</div>
```

## Responsive Design

Use Tailwind responsive prefixes:

```tsx
<Header
  className="w-full md:w-auto"
>
  Responsive Header
</Header>
```

## Best Practices

1. **Accessibility**: Always provide meaningful content or aria-label
4. **Composition**: Combine with other components using Stack or Flex for layouts
5. **Performance**: Avoid creating new callback functions on each render

## Related Components

- See your design system documentation for related components
